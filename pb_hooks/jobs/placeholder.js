/// <reference path="../../pb_data/types.d.ts" />

/**
 * Placeholder: Scheduled/cron job handlers.
 *
 * Example structure (to be implemented as needed):
 *
 * function cleanStaleSubscriptions(app) {
 *     // Find push subscriptions older than 90 days and delete
 * }
 *
 * module.exports = { cleanStaleSubscriptions }
 */

module.exports = {}
